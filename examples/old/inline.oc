// C traditionally uses this so naturally it is still allowed
static inline uint64_t add(uint64_t a, uint64_t b) {
  return a + b;
}

// However we should be easily be able to add static automatically
inline uint64_t add(uint64_t a, uint64_t b) {
  return a + b;
}
