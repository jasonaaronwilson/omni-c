// This file is based off of build.roci but is not meant to actually
// be run except for testing timing.

let foo = false;

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

/**
 * @function foo
 *
 * lorem ipsum dolor sit amet consectetur adipiscing elit nihil ut
 * nostrud quidem qui fugiat occaecat possimus sint deserunt minim est
 */
foo = fn(target_name, c_target_name, omni_c_binary, optimize) {

  if (string_equal(PLATFORM, "windows")) {
    target_name = string_append(target_name, ".exe");
  }

  build_reflection_header();
  build_roci_embedded_lib();

  if (all_older_than(target_name, ALL_OMNI_C_SRC)) {
    announce_no_build(target_name);
    return;
  } else {
    announce_build(target_name);
  }

  // let COMMAND_LINE = make_list("echo");
  let COMMAND_LINE = make_list(omni_c_binary);

  list_push(COMMAND_LINE, "generate-library");

  // Flags to omni-c generate-library
  list_push(COMMAND_LINE, "--c-output-file");
  list_push(COMMAND_LINE, c_target_name);
  // Do we still need this?
  list_push(COMMAND_LINE, "--omit-c-armyknife-include=true");

  list_push_all(COMMAND_LINE, ALL_OMNI_C_SRC);

  let result = shell(COMMAND_LINE);
  check_shell_command(result);

  let CC_COMMAND_LINE = make_list(CC);
  list_push_all(CC_COMMAND_LINE, CC_FLAGS);
  list_push_all(CC_COMMAND_LINE, WARN_FLAGS);
  list_push_all(CC_COMMAND_LINE, OPT_FLAGS);
  if (string_equal(PLATFORM, "darwin")) {
    list_push(CC_COMMAND_LINE, "-w"); // Turn off all warnings
    list_push(CC_COMMAND_LINE, "-I/opt/homebrew/opt/bdw-gc/include");
    list_push(CC_COMMAND_LINE, "-L/opt/homebrew/opt/bdw-gc/lib");
  }
  if (string_equal(PLATFORM, "windows")) {
    list_push(CC_COMMAND_LINE, "-Wno-deprecated-declarations");
    list_push(CC_COMMAND_LINE, "-Wno-pointer-sign");
    list_push(CC_COMMAND_LINE, "-Ic:/Users/jason/vcpkg/installed/x64-windows-static/include");
    list_push(CC_COMMAND_LINE, "c:/Users/jason/vcpkg/installed/x64-windows-static/lib/gc.lib");
    list_push(CC_COMMAND_LINE, "-ldbghelp");
    list_push(CC_COMMAND_LINE, "-lwinmm");
    list_push(CC_COMMAND_LINE, "-Wl,-debug");
  }

  list_push(CC_COMMAND_LINE, "-o");
  list_push(CC_COMMAND_LINE, target_name);

  // the output from omni-c is now the input to CC
  list_push(CC_COMMAND_LINE, c_target_name);

  if (not(string_equal(PLATFORM, "windows"))) {
    list_push(CC_COMMAND_LINE, "-lgc");
  }

  let cc_result = shell(CC_COMMAND_LINE);
  check_shell_command(cc_result);
};

exit(1);
