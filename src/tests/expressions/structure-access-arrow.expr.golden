{
    tag = PARSE_NODE_OPERATOR
    operator = "->"
    left = {
        tag = PARSE_NODE_IDENTIFIER
        token = var
    }
    right = {
        tag = PARSE_NODE_IDENTIFIER
        token = field_name
    }
}

// C Output
(var->field_name)
