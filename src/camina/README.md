# Camina IDE

This is a place-holder for a TUI based Emacs like text editor + IDE
written (mostly) in roci. The goal is not to replace Emacs, neovim,
etc., but provide a simple throw-back to early IDEs like Turbo C and
Quick C which might be useful for students.
