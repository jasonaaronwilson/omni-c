#!/usr/local/bin/chibi-scheme

; chibi-scheme notes:
;
; grab chibi-scheme from it's github repository and do make install (I
;  think)
;
; create a file that only contains () called ~/.snow/config.scm
; snow-chibi install '(slib pretty-print)'

(import (scheme small) (slib pretty-print))

(display (pretty-print->string (read) 80))
(exit)
