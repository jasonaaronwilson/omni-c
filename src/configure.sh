#!/bin/bash

### This isn't really a configuration script in the normal sense (at
### least right now) but should be run once you git clone this
### repoistory to initiate the boot-strap process.

exec ./bootstrap.sh "$@"
